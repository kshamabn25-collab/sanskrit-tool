name: Build & Release

# Releases are built from the private source repo (sanskrit-tool-src).
# This file is a placeholder — see the private repo for the actual workflow.

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Builds run from the private source repository."
